#!/bin/bash
# backcountryskitrace — staging environment
export ENV_NAME="staging"
export DEPLOY_BRANCH="${DEPLOY_BRANCH:-staging}"
# Update these once staging infra is set up
export DEPLOY_URL="${DEPLOY_URL:-}"
